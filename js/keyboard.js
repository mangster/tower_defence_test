/*
controls = {
                left: false,
                up: false,
                right: false,
                down: false
            };

window.onkeydown = function(e){
    var code = e.keyCode;
    e = e || window.event;
    if ( code === 37 ) {
    // Left key
        focus.vx = 0.1;
		focus.vz = -0.1;
    } else if ( code === 38 ) {
    // Up key
        focus.vx = 0.1;
		focus.vz = 0.1;
    } else if ( code === 39 ) {
    // Right key
        focus.vx = -0.1;
		focus.vz = 0.1;
    } else if ( code === 40 ) {
    // Down key
        focus.vx = -0.1;
		focus.vz = -0.1;
    }
};

window.onkeyup = function(e){
    e = e || window.event;
    var code = e.keyCode;
    if ( code === 37 ) {
    // Left key
        focus.vx = 0;
		focus.vz = 0;
    } else if ( code === 38 ) {
    // Up key
        focus.vx = 0;
		focus.vz = 0;
    } else if ( code === 39 ) {
    // Right key
        focus.vx = 0;
		focus.vz = 0;
    } else if ( code === 40 ) {
    // Down key
        focus.vx = 0;
		focus.vz = 0;
    }
        

};
*/